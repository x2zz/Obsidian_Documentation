---
title: 'Window'
description: 'The Window object is your base UI container. It hosts tabs, groupboxes and mostly everything else driven by the library.'
---

import { InlineTOC } from 'fumadocs-ui/components/inline-toc';
import { Tab, Tabs } from "fumadocs-ui/components/tabs";
import { TypeTable } from '@/components/type-table';
import { Accordion, Accordions } from 'fumadocs-ui/components/accordion';

<InlineTOC items={toc} />

---

## Window

![Obsidian](/images/obsidian/obsidian.png)

The `Window` object is your base UI container. It hosts tabs, groupboxes and mostly everything else driven by the library. The next step to using the library is to [create a tab inside the window](../structure/tabs).

### Usage
Create a window with `Library:CreateWindow()` and override any defaults you need:

```lua
local Window = Library:CreateWindow({
    Title = "mspaint",
    Footer = "version: example",
    Icon = 95816097006870,
    NotifySide = "Right",
})
```

<TypeTable
  type={{
    Title: {
      description: 'The title displayed at the top of the window',
      type: 'string',
      default: '"No Title"',
      required: true
    },
    Footer: {
      description: 'The text displayed at the bottom of the window',
      type: 'string',
      default: '"No Footer"',
      required: true
    },
    Position: {
      description: 'The initial position of the window',
      type: 'UDim2',
      default: 'UDim2.fromOffset(6, 6)',
      required: false
    },
    Size: {
      description: 'The size of the window',
      type: 'UDim2',
      default: 'UDim2.fromOffset(720, 600)',
      required: false
    },
    Center: {
      description: 'Whether to center the window on the screen',
      type: 'boolean',
      default: 'true',
      required: false
    },
    AutoShow: {
      description: 'Whether to show the window immediately',
      type: 'boolean',
      default: 'true',
      required: false
    },
    ToggleKeybind: {
      description: 'The keybind to toggle the UI',
      type: 'Enum.KeyCode',
      default: 'Enum.KeyCode.RightControl',
      required: false
    },
    NotifySide: {
      description: 'The side to show notifications ("Left" or "Right")',
      type: 'string',
      default: '"Right"',
      required: false
    },
    ShowCustomCursor: {
      description: 'Whether to show a custom cursor',
      type: 'boolean',
      default: 'true',
      required: false
    },
    Font: {
      description: 'The font to use for text',
      type: 'Enum.Font',
      default: 'Enum.Font.Code',
      required: false
    },
    CornerRadius: {
      description: 'The corner radius for UI elements',
      type: 'number',
      default: '4',
      required: false
    },
    Icon: {
      description: 'Optional icon for the window',
      type: 'string/ID',
      default: 'nil',
      required: false
    },
    IconSize: {
      description: 'Size of the icon if provided',
      type: 'UDim2',
      default: 'UDim2.fromOffset(30, 30)',
      required: false
    },
    BackgroundImage: {
      description: 'Background image for the window',
      type: 'string/ID',
      default: 'nil',
      required: false
    },
    BackgroundBlur: {
      description: 'Creates a BlurEffect in Lighting when the window opens. Tweens blur size 0 → 18 on show and back to 0 on hide.',
      type: 'boolean',
      default: 'false',
      required: false
    },
    TitleAnimation: {
      description: 'Slides the title holder in from -40px and fades in all text/images on launch.',
      type: 'boolean',
      default: 'false',
      required: false
    },
    IconAnimation: {
      description: 'Loops a 360° spin tween on the window icon container every 8 seconds.',
      type: 'boolean',
      default: 'false',
      required: false
    },
    AddGroupboxAnimation: {
      description: 'Sweeps an accent-colored glow line left → right across each groupbox every 3 seconds. ClipsDescendants is applied to the groupbox automatically.',
      type: 'boolean',
      default: 'false',
      required: false
    },
    Resizable: {
      description: 'Whether the window can be resized',
      type: 'boolean',
      default: 'true',
      required: false
    },
    ShowMobileButtons: {
      description: 'Show mobile buttons (Toggle/Lock buttons)',
      type: 'boolean',
      default: 'true',
      required: false
    },
    MobileButtonsSide: {
      description: 'Side to place mobile buttons on Load (Toggle/Lock buttons)',
      type: '"Left" | "Right"',
      default: '"Left"',
      required: false
    },
    DisableSearch: {
      description: 'Whether to disable the search box',
      type: 'boolean',
      default: 'false',
      required: false
    },
    SearchbarSize: {
      description: 'Size of the searchbar',
      type: 'UDim2',
      default: 'UDim2.fromScale(1, 1)',
      required: false
    },
    GlobalSearch: {
      description: 'Whether to enable a global search that searches in all the tabs.',
      type: 'boolean',
      default: 'false',
      required: false
    },
    UnlockMouseWhileOpen: {
      description: 'Whether to allow mouse movement while the Window is open even when the cursor\'s position is locked to the center (for example when in first person)',
      type: 'boolean',
      default: 'true',
      required: false
    },
    EnableSidebarResize: {
      description: 'Show a draggable handle so players can resize the sidebar',
      type: 'boolean',
      default: 'false',
      required: false
    },
    EnableCompacting: {
      description: 'Whether to allow compacting the sidebar',
      type: 'boolean',
      default: 'true',
      required: false
    },
    DisableCompactingSnap: {
      description: 'Whether to disable snapping while compacting the sidebar',
      type: 'boolean',
      default: 'false',
      required: false
    },
    SidebarCompacted: {
      description: 'Render the sidebar in compact mode (icons only) on first open',
      type: 'boolean',
      default: 'false',
      required: false
    },

    MinContainerWidth: {
      description: 'Minimum width of the main content container in pixels',
      type: 'number',
      default: '256',
      required: false
    },
    MinSidebarWidth: {
      description: 'Smallest width the sidebar can collapse to before switching into compact mode',
      type: 'number',
      default: '128',
      required: false
    },
    SidebarCompactWidth: {
      description: 'Width used while the sidebar is compacted',
      type: 'number',
      default: '48',
      required: false
    },
    SidebarCollapseThreshold: {
      description: 'Ratio threshold (0-1) that determines when the sidebar snaps into compact mode during drag',
      type: 'number',
      default: '0.5',
      required: false
    },
    CompactWidthActivation: {
      description: 'Width threshold that triggers compact mode when dragging the sidebar',
      type: 'number',
      default: '128',
      required: false
    },

    Animations: {
      description: 'Configuration table to enable animations',
      type: 'table',
      default: '{ ToggleWindow = false, TabSwitch = false, Groupbox = false, Dropdown = false, KeyPicker = false }',
      required: false
    },
    TabTransitionTime: {
      description: 'Duration of the tab swipe transition in seconds',
      type: 'number',
      default: '0.22',
      required: false
    },
    TabSwipeOffset: {
      description: 'Distance (in pixels) the tab slides during the switch animation',
      type: 'number',
      default: '26',
      required: false
    },
    TabSwipeFrom: {
      description: 'Direction the tab swipes in from ("left", "right", "top", "bottom")',
      type: 'string',
      default: '"bottom"',
      required: false
    },
  }}
/>

#### ChangeTitle
Changes the title of the window.

```lua
Window:ChangeTitle("New Title")
```

| Arg Idx | Argument Description | Type | Default |
| --- | --- | --- | --- |
| 1 | The new title of the window | string | nil |

#### SetFooter
Changes the footer of the window.

```lua
Window:SetFooter("New Footer")
```

| Arg Idx | Argument Description | Type | Default |
| --- | --- | --- | --- |
| 1 | The new footer of the window | string | nil |

#### SetBackgroundImage
Changes the background image of the window.

```lua
Window:SetBackgroundImage("rbxasset://textures/ui/GuiImagePlaceholder.png")
```

| Arg Idx | Argument Description | Type | Default |
| --- | --- | --- | --- |
| 1 | The new background image of the window | string/ID | nil |

#### SetCornerRadius
Changes the corner radius for elements.

```lua
Window:SetCornerRadius(10)
```

| Arg Idx | Argument Description | Type | Default |
| --- | --- | --- | --- |
| 1 | The new corner radius | number | nil |

#### Toggle

Toggles the visibility of the window.

```lua
Window:Toggle()
```

| Arg Idx | Argument Description | Type | Default |
| --- | --- | --- | --- |
| 1 | Force a specific visibility state | boolean? | nil |

#### ShowTabInfo

Displays the current tab's name and description in the header area (next to the searchbar).

```lua
Window:ShowTabInfo("Settings", "Configure your preferences")
```

| Arg Idx | Argument Description | Type | Default |
| --- | --- | --- | --- |
| 1 | Tab name to display | string | nil |
| 2 | Tab description to display | string | nil |

#### HideTabInfo

Hides the tab info display and restores the searchbar to full width.

```lua
Window:HideTabInfo()
```


### Sidebar Layout & Resizing

Obsidian can expose a draggable handle that lets players resize the sidebar at runtime. Enable it and tweak the behaviour directly from your window configuration:

```lua
local Window = Library:CreateWindow({
    Title = "mspaint",
    EnableSidebarResize = true,

    -- Optional Sidebar Settings
    MinSidebarWidth = 200, -- stop shrinking when the sidebar hits 200px
    SidebarCompactWidth = 56,
})
```

### Sidebar Methods

All window instances expose helpers for responding to layout changes or driving your own resizing logic.

#### GetSidebarWidth
Returns the current sidebar width in pixels.

```lua
local CurrentWidth = Window:GetSidebarWidth()
```

#### IsSidebarCompacted
Checks if the sidebar is currently in compact (icon-only) mode.

```lua
if Window:IsSidebarCompacted() then
    print("Sidebar is collapsed")
end
```

#### SetSidebarWidth
Programmatically resize the sidebar. Values outside the configured bounds are clamped automatically.

```lua
Window:SetSidebarWidth(240)
```

| Arg Idx | Argument Description | Type | Default |
| --- | --- | --- | --- |
| 1 | Desired sidebar width in pixels | number | `nil (required)` |

#### SetCompact
Toggle compact mode explicitly. Pass `true` to force compact, or `false` to restore the last expanded width.

```lua
Window:SetCompact(true)
```

| Arg Idx | Argument Description | Type | Default |
| --- | --- | --- | --- |
| 1 | Whether the sidebar should stay compacted | boolean | — |


### Animations

Obsidian has customizable, opt-in animations. You can enable them directly when creating the window:

```lua
local Window = Library:CreateWindow({
    Title = "mspaint",
    
    -- Enable animations
    Animations = {
        ToggleWindow = true,
        TabSwitch = true,
        Groupbox = true,
        Dropdown = true,
        KeyPicker = true
    },
    
    -- Customise Tab switching animation
    TabTransitionTime = 0.22,
    TabSwipeOffset = 26,
    TabSwipeFrom = "bottom"
})
```

#### Preview

<Tabs items={['ToggleWindow', 'TabSwitch', 'Groupbox', 'Dropdown', 'KeyPicker']}>
  <Tab value="ToggleWindow">
    Animation when opening or closing the UI window.
    ![ToggleWindow Preview](/images/obsidian/animations/toggle-window.gif)
  </Tab>
  <Tab value="TabSwitch">
    Transition effect when switching between different tabs (from bottom).
    ![TabSwitch Preview](/images/obsidian/animations/tab-switch.gif)
  </Tab>
  <Tab value="Dropdown">
    Animation when expanding or collapsing Dropdowns.
    ![Dropdown Preview](/images/obsidian/animations/dropdown.gif)
  </Tab>
  <Tab value="Groupbox">
    Animation when expanding or collapsing Groupboxes.
    ![Groupbox Preview](/images/obsidian/animations/groupbox.gif)
  </Tab>
  <Tab value="KeyPicker">
    Animation when interacting with KeyPickers.
    ![KeyPicker Preview](/images/obsidian/animations/keypicker.gif)
  </Tab>
</Tabs>


#### SetAnimations
Updates animation configuration.

```lua
Window:SetAnimations({ ToggleWindow = true, TabSwitch = true, Groupbox = true, Dropdown = true, KeyPicker = true }, 0.22, 26, "bottom")
```

| Arg Idx | Argument Description | Type | Default |
| --- | --- | --- | --- |
| 1 | The animations configuration table | table? | `nil` |
| 2 | Tab transition duration (seconds) | number? | `nil` |
| 3 | Tab swipe transition offset (pixels) | number? | `nil` |
| 4 | Tab swipe direction (`"left" \| "right" \| "top" \| "bottom"`) | string? | `nil` |


---

## Visual Features

Obsidian Modified adds opt-in visual features to `CreateWindow`. All default to `false` so existing scripts are unaffected.

```lua
local Window = Library:CreateWindow({
    Title = "My Hub",

    BackgroundBlur       = true,
    TitleAnimation       = true,
    IconAnimation        = true,
    AddGroupboxAnimation = true,
    -- Window outer glow is always active (no option needed)
})
```

### BackgroundBlur
Creates a `BlurEffect` instance in `game:GetService("Lighting")` when the window is first created. The blur size tweens from `0 → 18` when the window opens and back to `0` when it closes, matching `Library.WindowAnimationInfo`.

### TitleAnimation
On the first frame after the window is created, the `TitleHolder` frame is offset by `-40px` on the X axis and all child `TextLabel` / `ImageLabel` instances are set to fully transparent. A `0.6s Quad Out` tween then slides the holder back to its natural position and fades all children in.

### IconAnimation
Wraps the window icon inside a container frame and loops a `360°` rotation tween with `Linear` easing every `8 seconds`.

### Window Outer Glow
Always active — no option required. A sliced `ImageLabel` (`rbxassetid://6014261993`) is parented outside `MainFrame` at `ZIndex -1` with a `Position` offset of `-15px` on both axes and a `Size` padding of `+30px`. It pulses `ImageTransparency` between `0.4` and `0.2` on a `Sine InOut` infinite tween with a `2 second` cycle.

### AddGroupboxAnimation
Injects a `50px` wide accent-colored `Frame` into every groupbox. The frame sweeps from the left edge to the right edge over `2 seconds` then resets, repeating every `3 seconds`. `ClipsDescendants` is automatically set on the groupbox holder.
